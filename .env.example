# ============================================================================
# Sonatype IQ Server Version Checker - Environment Configuration Template
# ============================================================================
# Copy this file to .env and fill in your actual values
# Command: cp .env.example .env
#
# IMPORTANT: Add .env to .gitignore to prevent committing credentials!
# ============================================================================

# ----------------------------------------------------------------------------
# Feature Flags - Enable/Disable Notification Channels
# ----------------------------------------------------------------------------
# Set to 'true' to enable each notification method
# You can enable multiple channels simultaneously

ENABLE_EMAIL=false
ENABLE_SLACK=false
ENABLE_TEAMS=false

# ----------------------------------------------------------------------------
# Email Configuration (Required if ENABLE_EMAIL=true)
# ----------------------------------------------------------------------------
# SMTP server settings for sending email notifications

# SMTP server hostname (default: smtp.office365.com for Microsoft 365)
SMTP_SERVER=smtp.office365.com

# SMTP server port (default: 587 for STARTTLS)
SMTP_PORT=587

# Email account credentials
EMAIL_USERNAME=monitor@example.com
EMAIL_PASSWORD=your_email_password_here

# Email addresses
EMAIL_FROM=monitor@example.com
EMAIL_TO=team@example.com

# Multiple recipients (comma-separated)
# EMAIL_TO=admin1@example.com,admin2@example.com,team@example.com

# ----------------------------------------------------------------------------
# Slack Configuration (Required if ENABLE_SLACK=true)
# ----------------------------------------------------------------------------
# Slack Incoming Webhook URL
# Get this from: Slack App > Incoming Webhooks > Add New Webhook to Workspace

SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Example:
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/T00000000/B00000000/XXXXXXXXXXXXXXXXXXXX

# ----------------------------------------------------------------------------
# Microsoft Teams Configuration (Required if ENABLE_TEAMS=true)
# ----------------------------------------------------------------------------
# Teams webhook URL - Supports two methods:
#
# Method 1: Power Automate Flow (Recommended - More Flexible)
#   - Create a Flow with "When a HTTP request is received" trigger
#   - Copy the HTTP POST URL
#
# Method 2: Traditional Incoming Webhook Connector
#   - In Teams channel: ... > Connectors > Incoming Webhook
#   - Configure and copy the webhook URL

TEAMS_WEBHOOK_URL=https://your-webhook-url-here

# Example (Power Automate):
# TEAMS_WEBHOOK_URL=https://prod-00.eastus.logic.azure.com:443/workflows/abc123.../triggers/manual/paths/invoke?api-version=2016-10-01&sp=...

# Example (Traditional Webhook):
# TEAMS_WEBHOOK_URL=https://your-org.webhook.office.com/webhookb2/abc123@def456/IncomingWebhook/xyz789/...

# ----------------------------------------------------------------------------
# General Settings
# ----------------------------------------------------------------------------

# File to store the last known version (default: sonatype_last_version.txt)
VERSION_FILE=sonatype_last_version.txt

# Dry run mode - test without sending notifications (default: false)
DRY_RUN=false

# ----------------------------------------------------------------------------
# Common SMTP Server Examples
# ----------------------------------------------------------------------------
# 
# Microsoft 365 / Outlook.com:
#   SMTP_SERVER=smtp.office365.com
#   SMTP_PORT=587
#
# Gmail (requires App Password if 2FA enabled):
#   SMTP_SERVER=smtp.gmail.com
#   SMTP_PORT=587
#
# Yahoo Mail:
#   SMTP_SERVER=smtp.mail.yahoo.com
#   SMTP_PORT=587
#
# SendGrid:
#   SMTP_SERVER=smtp.sendgrid.net
#   SMTP_PORT=587
#
# Amazon SES:
#   SMTP_SERVER=email-smtp.us-east-1.amazonaws.com
#   SMTP_PORT=587
#
# ----------------------------------------------------------------------------

# ----------------------------------------------------------------------------
# Security Notes
# ----------------------------------------------------------------------------
# 1. NEVER commit .env file to version control
# 2. Keep file permissions restrictive: chmod 600 .env
# 3. Use dedicated service accounts with minimal permissions
# 4. Rotate credentials regularly (every 90 days recommended)
# 5. Use app-specific passwords when available (e.g., Gmail, Microsoft 365)
# 6. For production, consider using secret management systems:
#    - AWS Secrets Manager
#    - Azure Key Vault
#    - HashiCorp Vault
#    - Kubernetes Secrets
# ----------------------------------------------------------------------------